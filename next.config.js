/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['www.biovie.fr'], // Add any other domains you use for images
    unoptimized: true
  }
}

module.exports = nextConfig